# CLAUDE.md

This file provides guidance to Claude Code when working with this project.

## Project Overview

Wagile project: ModelCompare

## Project Structure

```
ModelCompare/
├── .claude/
│   ├── agents/       -> symlinks to Framework agents
│   └── skills/       -> symlinks to Framework skills
├── docs/
├── backlog.md        (template, ready for /lead)
├── ideas.md          (feedback loop)
├── technical_spec.md (pending)
├── ui_spec.md        (pending)
├── CLAUDE.md         (project config)
└── .git/
```

## Development Workflow

1. **Before making changes:** Read relevant files to understand context
2. **When implementing features:** Follow existing patterns in the codebase
3. **Before committing:** Run tests and linting
4. **Security:** Never hardcode secrets; use environment variables

## Key Files

- `backlog.md` - Product backlog (managed by PO Agent)
- `technical_spec.md` - Architecture and technical decisions (generated by Architect Agent)
- `ui_spec.md` - UI/UX specification (generated by UX Designer Agent, if applicable)
- `ideas.md` - Feedback loop, processed at the start of each /lead session
